import { Connection } from '@solana/web3.js';
import { ReadApiConnection } from '@metaplex-foundation/js/dist/types/utils/readApiConnection';
import { MetaplexPlugin, Cluster } from '@metaplex-foundation/js/dist/types/types';
export type MetaplexOptions = {
    cluster?: Cluster;
};
export declare class Metaplex {
    /** The connection object from Solana's SDK. */
    readonly connection: Connection | ReadApiConnection;
    /** The cluster in which the connection endpoint belongs to. */
    readonly cluster: Cluster;
    constructor(connection: Connection, options?: MetaplexOptions);
    static make(connection: Connection, options?: MetaplexOptions): Metaplex;
    use(plugin: MetaplexPlugin): this;
}
